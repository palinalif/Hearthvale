# Overnight Sequence Test — Task A

This is task A of the sequential overnight orchestration test. It is a minimal, isolated
documentation change used to verify that each overnight task can be scoped to a single
file, committed in its own worktree, and verified as clean by the verifier before the
next task in the sequence runs.

Re-run note (2026-09-21): this is an idempotent re-run of task A in the sequential
overnight orchestration test; the original note above is preserved unchanged.
